{
  "name": "@reprorelay/react-demo",
  "version": "0.1.0",
  "private": true,
  "description": "React demo app embedding ReproRelay.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && vite build",
    "dev": "vite --host 0.0.0.0 --port 3000",
    "lint": "eslint src",
    "preview": "vite preview --host 0.0.0.0 --port 3000",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@reprorelay/react": "0.1.1",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.3",
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2",
    "vite": "^8.1.4"
  }
}
